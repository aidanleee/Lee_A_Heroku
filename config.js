const config = {
    user: "root",
    pword: "root", // windows leave this blank
    host: "localhost",
    port: "8889", // windos users - sql port is 3306
    database: "macbook"
}

module.exports = config;